% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_oakley.R
\name{apply_oakley}
\alias{apply_oakley}
\title{Apply the Oakley algorithm}
\usage{
apply_oakley(agdb, threshold = 40)
}
\arguments{
\item{agdb}{A \code{tibble} of activity data. Its epoch length must be 15, 30,
60, or 120 seconds.}

\item{threshold}{Numeric wake threshold, or \code{"automatic"}. An epoch is
scored as awake when its weighted activity score is greater than the
threshold. The default is 40.}
}
\value{
A \code{tibble} of activity data. A new column \code{sleep} indicates whether
each epoch is scored as asleep (\code{"S"}) or awake (\code{"W"}).
}
\description{
The Oakley sleep scoring algorithm classifies each epoch as asleep or awake
from wrist activity counts. It was developed for the Actiwatch activity
monitoring system.
}
\details{
Oakley's weighted activity score is the sum of activity counts in a window
centred on the epoch, multiplied by epoch-length-specific weights. For
60-second data, the score at epoch \code{t} is

\if{html}{\out{<div class="sourceCode">}}\preformatted{0.04 * count[t - 2] + 0.20 * count[t - 1] + count[t] +
  0.20 * count[t + 1] + 0.04 * count[t + 2]
}\if{html}{\out{</div>}}

The method uses the \code{axis1} count directly. The weights for 15-, 30-, and
120-second epochs are those specified in the Actiwatch software manual.
Counts outside the observed series are treated as zero when scoring the
first and last epochs.

With \code{threshold = "automatic"}, the wake threshold is calculated separately
for each group as \code{0.88888 * sum(count) / mobile_time_minutes}. An epoch is
mobile when its count is at least the number of 15-second intervals in that
epoch. \code{mobile_time_minutes} is the number of mobile epochs multiplied by
the epoch length in minutes.
}
\examples{
library("dplyr")
data("gtxplus1day")

gtxplus1day \%>\%
  collapse_epochs(60) \%>\%
  apply_oakley()
}
\references{
Oakley NR. \emph{Validation with Polysomnography of the Sleepwatch Sleep/Wake
Scoring Algorithm Used by the Actiwatch Activity Monitoring System}.
Technical Report. Mini-Mitter; 1997.

Actiwatch Communication and Sleep Analysis Software instruction manual.
Respironics, Inc. Available at
\url{https://fccid.io/JIAAWR1/Users-Manual/USERS-MANUAL-1-920937}.

The implementation and weights were cross-checked against the
\href{https://ghammad.github.io/pyActigraphy/_autosummary/pyActigraphy.sleep.ScoringMixin.Oakley.html}{pyActigraphy Oakley documentation}.
Its \href{https://ghammad.github.io/pyActigraphy/_modules/pyActigraphy/sleep/scoring_base.html#ScoringMixin.Oakley}{source implementation}
was also consulted.
}
\seealso{
\code{\link[=apply_sadeh]{apply_sadeh()}}, \code{\link[=apply_cole_kripke]{apply_cole_kripke()}}
}
